# =============================================================================
# 1C AI Assistant Middleware — облачный профиль (Docker)
# =============================================================================
# Облегчённый набор без ML-зависимостей и без dev-инструментов (pytest и т. п.).
# Используется в Dockerfile для production-сборки. Версии соответствуют основному
# requirements.txt, проверены в venv 3.12 на 16 мая 2026 г.
# =============================================================================

# --- Веб-фреймворк ---
fastapi==0.136.1
uvicorn==0.47.0
starlette==1.0.0
python-multipart==0.0.28

# --- Валидация и сериализация ---
pydantic==2.13.4
pydantic_core==2.46.4

# --- LLM-провайдер ---
openai==1.109.1

# --- ORM и БД ---
SQLAlchemy==2.0.49

# --- Авторизация и безопасность ---
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt<4
cryptography==48.0.0

# --- Окружение ---
python-dotenv==1.2.2

# --- ML / эмбеддинги (минимальный набор) ---
numpy==2.4.5
# В текущей реализации word-overlap fallback не включается без загруженного
# массива embeddings из-за ранней проверки в metadata_service. До исправления
# облачный профиль без sentence-transformers фактически работает без RAG.
# Для полноценного семантического поиска добавить отдельной командой:
#     pip install sentence-transformers
