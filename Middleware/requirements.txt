# =============================================================================
# 1C AI Assistant Middleware — Python зависимости
# =============================================================================
# Версии зафиксированы по результатам pip freeze в чистом venv (Python 3.12.10)
# на 26 мая 2026 г. На 30 августа 2026 г. все 46 обнаруживаемых тестов
# middleware проходят на этом наборе.
# Для установки в чистом venv:
#     python -m venv .venv
#     .venv\Scripts\python.exe -m pip install --upgrade pip
#     .venv\Scripts\python.exe -m pip install -r requirements.txt
# =============================================================================

# --- Веб-фреймворк ---------------------------------------------------------
fastapi==0.136.1
uvicorn==0.47.0
starlette==1.0.0
python-multipart==0.0.28          # для form-data в эндпоинтах FastAPI

# --- Валидация и сериализация ---------------------------------------------
pydantic==2.13.4
pydantic_core==2.46.4

# --- LLM-провайдер ---------------------------------------------------------
openai==1.109.1                   # совместимый клиент для Fireworks AI и OpenRouter

# --- ORM и БД --------------------------------------------------------------
SQLAlchemy==2.0.49

# --- Авторизация и безопасность -------------------------------------------
python-jose[cryptography]==3.5.0  # JWT-токены
passlib[bcrypt]==1.7.4            # хеширование паролей (обёртка над bcrypt)
bcrypt<4                          # ВАЖНО: passlib 1.7.4 несовместим с bcrypt 4.x
                                   # (отсутствует атрибут bcrypt.__about__)
                                   # фиксируется на bcrypt 3.x (актуальная 3.2.2)
cryptography==48.0.0
slowapi==0.1.9                    # rate limiting (защита от брутфорса /auth/login)

# --- Окружение -------------------------------------------------------------
python-dotenv==1.2.2

# --- ML / эмбеддинги -------------------------------------------------------
numpy==2.4.5                      # нужен для metadata_service (косинусное сходство)
# sentence-transformers           # опционально: при отсутствии metadata_service
# torch                           #   падает на word-overlap fallback. Установка
                                  #   тяжёлая (≈ 2–4 ГБ), производится отдельно
                                  #   командой:
                                  #     pip install sentence-transformers
                                  #   модель paraphrase-multilingual-MiniLM-L12-v2
                                  #   подгружается с HuggingFace при первом запуске.

# --- HTTP-клиент (для тестов FastAPI TestClient) --------------------------
httpx==0.28.1

# --- Тестирование ----------------------------------------------------------
pytest==9.0.3
pytest-asyncio==1.3.0
