# ПРОТОКОЛ ПЕРЕДАЧИ СОСТОЯНИЯ И КОНТЕКСТА (CODEX -> ANTIGRAVITY IDE)
**Дата генерации:** 02.09.2026 | **Проект:** Интеллектуальный ассистент и Канбан-доска для 1С:УНФ 3.0

---

## 1. ЦЕЛЬ И ТЕКУЩИЙ ФОКУС РАБОТЫ
- **Главная цель:** Создание и доведение до 100% работоспособности интеллектуального ассистента в среде «1С:Управление нашей фирмой 3.0» (1С:УНФ) с интеграцией FastAPI Middleware и LLM (Google Gemini).
- **Текущий этап:** **Практическое сквозное тестирование и отладка программного кода** 5 ключевых бизнес-сценариев автоматизации в 1С (НЕ написание текста диплома, НЕ подготовка презентации).
- **Среда выполнения:**
  - Платформа 1С: `8.5.1.1302` (информационная база `C:\Users\kobza\Documents\1C\SmallBusinessDemo`);
  - Сервер Middleware: `http://127.0.0.1:8000` (FastAPI / Python 3.12 daemon);
  - Рабочая директория: `D:\Share\Projects\diplom`.

---

## 2. КАРТА КОДОВОЙ БАЗЫ И ФАЙЛОВ
- `1c_extensions/1C_Full_Code_v6.bsl` — монолитный исходный код формы «Чат с ассистентом» (78 процедур/функций).
- `1c_extensions/ai_assistant_src/DataProcessors/ЧатСАссистентом/Forms/Форма/Ext/Form/Module.bsl` — синхронная копия модуля формы для сборки.
- `1c_extensions/1C_Kanban_Extension.bsl` и `1c_extensions/kanban_board_src/` — исходники обработки «Канбан-доска».
- `scratch/compile_epfs.py` — скрипт автоматической сборки EPF-файлов через штатную утилиту `1cv8.exe` и копирования на Рабочий стол.
- `C:\Users\kobza\Desktop\ЧатСАссистентом.epf` и `C:\Users\kobza\Desktop\КанбанДоска.epf` — скомпилированные внешние обработки для пользователя.
- `Middleware/app/services/llm_service.py` — сервис маршрутизации, быстрый классификатор `_fast_classify_intent` (<10 мс), валидация действий `VALID_ACTIONS`, отказоустойчивый пул моделей (`gemini-2.5-flash`, `gemini-1.5-flash`, `gemini-2.0-flash`).
- `Middleware/app/services/metadata_service.py` — гибридный RAG по метаданным 1С (BM25 + эмбеддинги).
- `Middleware/main.py` — точка входа FastAPI сервера.

---

## 3. СТАТУС 5 ИНТЕЛЛЕКТУАЛЬНЫХ БИЗНЕС-СЦЕНАРИЕВ
1. **Сценарий 1: Парсинг неструктурированной заявки (`parse_order_text`)**
   - *Запрос:* `«Выстави счет для АльфаМарт: Клей ПВХ 5 шт по 350 руб, Кабель 100 м по 120 руб»`
   - *Статус:* **УСПЕШНО ПРОТЕСТИРОВАН В 1С (PASSED)**.
   - *Результат:* Создан «Заказ покупателя №4» для клиента «АЛЬФАМАРТ», корректно подобраны позиции («Клей ПВХ», «Кабель для интернета»), рассчитаны суммы, открыта форма документа.

2. **Сценарий 2: Генерация писем должникам (`generate_debt_letters`)**
   - *Запрос:* `«Подготовь письма должникам с напоминанием об оплате»`
   - *Статус:* **В ПРОЦЕССЕ ТЕСТИРОВАНИЯ (READY FOR RETEST)**.
   - *Выполненные правки:* Устранены ошибки `.Свойство(...)` у параметров URL, метод открытия карточек переведен на `ПоказатьЗначение(, Ссылка)`, добавлена функция `НайтиКонтрагентаПоИмениНаСервере`, добавлено подтверждение создания задачи на звонок в чат.

3. **Сценарий 3: Заказ поставщику по дефициту склада (`create_procurement_order`)**
   - *Запрос:* `«Сформируй заказ поставщику по дефициту на складе»`
   - *Статус:* **ГОТОВ К ТЕСТИРОВАНИЮ (READY TO TEST)**.
   - *Логика:* Сравнивает резервы в `ЗаказыПокупателей` и остатки в `Запасы`, формирует документ `ЗаказПоставщику` с обязательными полями УНФ (`СостояниеЗаказа`, `ВалютаДокумента`, `ПоложениеСклада`) и ставит задачу снабженцу в Канбан.

4. **Сценарий 4: Коммерческое предложение со скидкой (`generate_commercial_proposal`)**
   - *Запрос:* `«Составь коммерческое предложение для АльфаМарт по осветительному оборудованию»`
   - *Статус:* **ГОТОВ К ТЕСТИРОВАНИЮ (READY TO TEST)**.
   - *Логика:* Рассчитывает скидку 7%, формирует интерактивное КП с кнопкой мгновенного перевода КП в «Заказ покупателя».

5. **Сценарий 5: Предиктивный аудит зависших заказов (`audit_stalled_orders`)**
   - *Запрос:* `«Проверь зависшие заказы и узкие места воронки»`
   - *Статус:* **ГОТОВ К ТЕСТИРОВАНИЮ (READY TO TEST)**.
   - *Логика:* Находит открытые заказы без движения >3 дней, рассчитывает сумму финансового риска и выводит кнопки оперативного реагирования.

---

## 4. КЛЮЧЕВЫЕ ВЫЯВЛЕННЫЕ ОСОБЕННОСТИ И РЕШЕНИЯ ДЕБАГА
- **1С:УНФ 3.0 Договоры:** Реквизит `Контрагент.ОсновнойДоговорКонтрагента` отсутствует. Договор всегда извлекается запросом: `ВЫБРАТЬ Ссылка ИЗ Справочник.ДоговорыКонтрагентов ГДЕ Владелец = &Владелец И НЕ ПометкаУдаления`.
- **Реквизиты документов УНФ:** В «Заказе покупателя» поле контрагента называется `Контрагент` (попытка записи в `Покупатель` вызывает ошибку). Обязательно заполнять `СостояниеЗаказа`, `ВалютаДокумента`, `Курс = 1`, `Кратность = 1`, `ПоложениеСклада = ВШапке`.
- **Открытие карточек 1С:** Вместо ненадежного `ОткрытьФорму("...ФормаОбъекта")` используется платформенный метод `ПоказатьЗначение(, Ссылка)`.
- **Параметры ссылок в HTML:** Для безопасного извлечения параметров используется клиентская функция `ШПолучитьПараметр(Параметры, Ключ, ПоУмолчанию)`.
- **FastAPI / LLM:** В классе `LLMService` объявлен классовый атрибут `VALID_ACTIONS`. Быстрый путь `_fast_classify_intent` обрабатывает бизнес-команды за $<10$ мс.

---

## 5. ПРАВИЛА И ОГРАНИЧЕНИЯ (ПРИОРИТЕТ 1)
1. Строгий академический стиль, кавычки-елочки («»), списки с маленькой буквы и точкой с запятой (;).
2. Ссылки на рисунки в конце предложения в скобках: `(рисунок 3.1)`.
3. Запрещено завершать подразделы рисунками или таблицами.
4. Запрещено использовать команды терминала при общении с пользователем.
5. Использовать только реальные метаданные конфигурации 1С:УНФ 3.0.
