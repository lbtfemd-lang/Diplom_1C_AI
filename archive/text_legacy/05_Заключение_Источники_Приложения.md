ЗАКЛЮЧЕНИЕ

В ходе выполнения дипломного проекта разработан прототип, состоящий из двух расширений конфигурации 1С:УНФ: «Интеллектуальный ассистент» и «Канбан‑доска», обеспечивающий диалоговый интерфейс для консультаций, выполнения типовых действий и управления задачами в учетной системе. Для достижения цели реализованы:
- клиентская часть в 1С — два расширения: «Интеллектуальный ассистент» (чат‑интерфейс на основе таблицы формы с условным оформлением, быстрые кнопки, отправка запросов по HTTP, исполнение команд) и «Канбан‑доска» (интерактивная визуализация задач, открытие из чата через `ОткрытьФорму`), совместимость с мобильным клиентом «1С:Предприятие»;
- серверная часть (Python middleware на FastAPI), выполняющая обращение к LLM, семантический поиск по метаданным базы, авторизацию пользователей с ролевой моделью (admin/manager/employee) и управление канбан‑задачами;
- система авторизации на основе JWT‑токенов с хранением в SQLite, ролевой фильтрацией данных и автоматической авторетрай при устаревании токена;
- канбан‑доска для управления задачами: создание, просмотр, перемещение между колонками, отслеживание просроченных задач, фильтрация по ролям и департаментам, экспорт в CSV;
- механизм «универсального поиска», позволяющий ассистенту учитывать реальный состав объектов конфигурации и их реквизиты;
- протокол команд `action/data`, обеспечивающий контролируемое и безопасное выполнение операций в 1С.

Количественные результаты тестирования

Результаты тестирования на типовых сценариях (46 автоматизированных тестов: 14 — авторизация, 5 — чат и метаданные, 10 — канбан, 17 — интеграция и постобработка 1С) и 10 ручных сценариев подтверждают работоспособность и корректность реализации. Все 46 автоматизированных тестов пройдены успешно, аварийных завершений формы 1С не зафиксировано. Среднее время ответа ассистента на типовые запросы составляет 4–6 секунд, из которых ~50 мс занимает RAG‑поиск по локальному индексу, ~4–5 секунд — обращение к LLM‑провайдеру, ~100 мс — сетевая передача данных. Все 10 функциональных требований (FR‑01…FR‑10) покрыты тестами на 100 %.

Таблица — Сравнение трудозатрат до и после внедрения ассистента (оценка на основе типовых сценариев)

| Операция | До внедрения | После внедрения | Снижение |
|---|---|---|---|
| Поиск формы отчёта (например, «Валовая прибыль») | 3–5 мин (навигация по меню) | 10 сек (ввод запроса) | ~95 % |
| Проверка статуса документа по номеру | 1–2 мин (открытие списка, поиск) | 15 сек (запрос в чате) | ~85 % |
| Создание заказа с проверкой контрагента | 8–10 мин (с учётом ошибок) | 3–4 мин (ассистент подсказывает) | ~60 % |
| Формирование списка должников | 2–3 мин (открытие отчёта, настройка параметров) | 15 сек (запрос «должники») | ~90 % |
| Постановка задачи сотруднику | 5 мин (устно/мессенджер, нет контроля) | 30 сек (канбан‑задача с дедлайном) | ~90 % |

Ожидаемый эффект от внедрения в ООО «ЛюксонДекор»: сокращение числа обращений в поддержку по 1С с ~60–70 в месяц до ~15–20 (за счёт консультационного режима и навигации); снижение среднего времени поиска форм и отчётов с 3–5 минут до 10–15 секунд; уменьшение ошибок при создании документов за счёт подсказок ассистента по обязательным реквизитам; формализация контроля задач через канбан‑доску вместо устных поручений.

Перспективы развития проекта:
- Расширение набора команд и сценариев, включая поддержку табличных частей документов (например, заполнение спецификации заказа несколькими позициями), многошаговых диалогов (ассистент уточняет параметры при неоднозначности запроса) и голосового ввода на мобильном клиенте.
- Полноценное мобильное приложение на базе 1С:Предприятие Mobile с публикацией базы на веб‑сервере, включающее оба расширения (чат и канбан‑доска с адаптированным интерфейсом), push‑уведомления о просроченных задачах и авторизацию по отпечатку/PIN.
- Оптимизация хранения векторного индекса и семантического поиска для высоконагруженных систем. При масштабировании решения на конфигурации уровня 1С:ERP (содержащие свыше 10 000 объектов метаданных и регистров) планируется переход от хранения индекса в оперативной памяти в формате .npy к использованию специализированных векторных баз данных, таких как pgvector (расширение для PostgreSQL) или распределенной векторной СУБД Qdrant. Это позволит применять алгоритмы приближенного поиска ближайших соседей (HNSW) для субмиллисекундного отклика, осуществлять сегментацию векторного пространства (Multi-tenancy) по базам данных отдельных филиалов или юридических лиц и поддерживать динамическое обновление индекса без перезагрузки процессов Middleware.
- Переход от локальной однопользовательской архитектуры к распределенной горизонтально масштабируемой корпоративной инфраструктуре. Вместо одиночного инстанса FastAPI с СУБД SQLite предполагается контейнеризация Middleware на базе Docker под управлением Kubernetes. Это обеспечит автоматическое масштабирование веб-сервисов, балансировку входящих запросов с помощью Nginx или HAProxy, централизованное шифрование трафика (HTTPS/TLS) и интеграцию с корпоративными каталогами пользователей Active Directory (AD/LDAP). Хранение данных переносится на кластер PostgreSQL с поддержкой Read-реплик, а внедрение кэширующего слоя на базе Redis для JWT-сессий, счетчиков лимитов запросов (rate limiting) и частых однотипных ответов LLM позволит минимизировать нагрузку на внутренний GPU-сервер вывода.
- Частичная локализация обработки: при строгих требованиях к конфиденциальности (данные не должны покидать контур организации) возможна замена облачного LLM на локальную модель (например, Llama 3 70B или Qwen 2.5 72B), развернутую на собственном GPU-сервере (vLLM / TensorRT-LLM). Это обеспечивает полное сохранение коммерческой тайны внутри корпоративной сети предприятия.
- Внедрение расширенного аудита и журналирования: сохранение всех запросов пользователей, ответов ассистента и выполненных действий в отдельную таблицу СУБД с возможностью генерации отчётов для службы информационной безопасности; добавление автоматического маскирования чувствительных данных (ИНН, номера счетов) перед отправкой в LLM.

Вывод о соответствии цели и задачам

Поставленная в работе цель — разработать и внедрить прототип расширения «Интеллектуальный ассистент» для 1С:УНФ — достигнута. Все задачи, сформулированные во введении, выполнены: проведён анализ предметной области (глава 1), выполнен обзор аналогов и сформулированы требования, разработана архитектура (глава 2), реализован прототип (глава 3), выполнено тестирование, подтверждающее работоспособность решения. Результаты тестирования показывают, что предложенный подход снижает трудоемкость навигации, ускоряет выполнение рутинных операций и обеспечивает управляемый доступ к функциям системы.

СПИСОК ИСПОЛЬЗОВАННЫХ ИСТОЧНИКОВ

1. Платформа «1С:Предприятие 8.3». Руководство разработчика (документация 1С).
2. Механизм расширений конфигурации 1С:Предприятия (документация 1С).
3. Синтаксис встроенного языка 1С (официальная документация).
4. 1С:Предприятие Mobile. Руководство разработчика мобильных приложений (документация 1С).
5. Конфигурация «1С:Управление нашей фирмой». Руководство пользователя (документация 1С).
6. FastAPI. Документация по разработке REST API на Python [Электронный ресурс]. — Режим доступа: https://fastapi.tiangolo.com.
7. Uvicorn. ASGI‑сервер для Python‑приложений (документация).
8. Pydantic. Валидация данных и модели для Python (документация).
9. Fireworks AI. OpenAI‑совместимый API для чат‑комплишенов [Электронный ресурс]. — Режим доступа: https://app.fireworks.ai.
10. Библиотека `openai` для Python (документация).
11. Sentence‑Transformers. Документация по получению семантических эмбеддингов.
12. NumPy. Документация по работе с массивами и предвычисленными эмбеддингами (.npy).
13. SQLAlchemy. Документация по ORM для Python [Электронный ресурс]. — Режим доступа: https://www.sqlalchemy.org.
14. python‑jose. Библиотека для работы с JWT‑токенами в Python (документация).
15. bcrypt / passlib. Библиотеки для безопасного хеширования паролей (документация).
16. paraphrase-multilingual-MiniLM-L12-v2. Модель для получения мультиязычных семантических эмбеддингов (sentence-transformers) [Электронный ресурс]. — Режим доступа: https://huggingface.co/sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2.
17. JSON (RFC 8259). Формат обмена данными.
18. JSON Web Tokens (RFC 7519). Стандарт токенной аутентификации.
19. The OAuth 2.0 Authorization Framework: Bearer Token Usage (RFC 6750).
20. ГОСТ Р 57580.1-2017. Информационная безопасность финансовых организаций. Общие положения.
21. ГОСТ Р ИСО/МЭК 27001-2021. Системы менеджмента информационной безопасности. Требования.
22. Lewis P., Perez E., Piktus A. и др. Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks // Advances in Neural Information Processing Systems (NeurIPS). 2020. Vol. 33. P. 9459–9474.
23. Reimers N., Gurevych I. Sentence-BERT: Sentence Embeddings using Siamese BERT-Networks // Proceedings of the 2019 Conference on Empirical Methods in Natural Language Processing (EMNLP). 2019. P. 3982–3992.
24. Anderson D.J. Kanban: Successful Evolutionary Change for Your Technology Business. Blue Hole Press, 2010. 278 p.
25. Brown T.B., Mann B. и др. Language Models are Few-Shot Learners // Advances in Neural Information Processing Systems (NeurIPS). 2020. Vol. 33. P. 1877–1901.
26. Starlette. ASGI framework / toolkit (документация) [Электронный ресурс]. — Режим доступа: https://www.starlette.io.
27. Python Software Foundation. The Python Language Reference (документация) [Электронный ресурс]. — Режим доступа: https://docs.python.org/3/reference/.
28. Основы информационной безопасности при интеграции внешних сервисов в корпоративные ИС (учебные материалы).

ПРИЛОЖЕНИЯ (рекомендуемое наполнение)

Приложение А — Исходный код клиентской части (1С, модуль формы чата, `1C_Full_Code_v6.bsl`). Содержит: процедуры инициализации формы (`ПриСозданииНаСервере`), отправки сообщения (`ОтправитьСообщение`), генерации HTML‑чата (`СгенерироватьЧатHTML`, `СгенерироватьЧатHTMLЗагрузка`), диспетчер команд (`ВыполнитьКомандуАссистента` с ветвлением по action: `open_list`, `find_object`, `create_object`, `get_status`, `get_stock`, `get_debtors`, `run_analytics`, `create_kanban_task`, `show_kanban`), цепочка создания объекта (`Шаг1_ПроверкаКлиента`, `Шаг2_ПроверкаНоменклатуры`, `Шаг3_Финал`), серверные функции‑помощники, общий блок шаблонных HTTP/Auth/JSON‑функций (ШАдресСервера, ШПолучитьТокенАвторизации, ШВыполнитьHTTPЗапрос, ШСериализоватьВJSON, ШДесериализоватьИзJSON и др.).

Приложение А1 — Исходный код расширения «Канбан‑доска» (`1C_Kanban_Extension.bsl`). Содержит: процедуры обновления доски (`ОбновитьДоску`, `ПолучитьДоскуНаСервереВызов`), серверную генерацию HTML‑визуализации канбан (`СгенерироватьКанбанHTML`), обработчик кликов по карточкам (`КанбанHTMLНажатиеНаСсылку`), CRUD‑операции (`СоздатьЗадачуНаСервере`, `ПереместитьЗадачуНаСервере`, `УдалитьЗадачуНаСервере`), утилиты (`ЭкранироватьHTML`), общий блок шаблонных функций.

Приложение Б — Исходный код серверной части (Python middleware: `main.py`, `llm_service.py`, `metadata_service.py`). Содержит: инициализацию FastAPI‑приложения и регистрацию эндпоинтов (`/chat`, `/auth/login`, `/auth/me`, `/auth/register`, `/auth/users`, `/departments`, `/kanban/tasks`, `/kanban/board`, `/kanban/stats`, `/kanban/notifications`, `/kanban/tasks/export`, `/kanban/sync/1c`, `/update_metadata`, `/health`), формирование system prompt, запрос к модели GLM‑5.1, многоуровневый разбор JSON, пост‑обработку намерений, загрузку и поиск по индексу метаданных (cosine similarity / word overlap).

Приложение В — Исходный код авторизации (`auth_service.py`, `auth_models.py`). Содержит: регистрацию и логин пользователей, хеширование паролей (bcrypt), формирование и верификацию JWT‑токенов (python‑jose, HS256), CRUD пользователей и департаментов, ролевую модель (admin/manager/employee), seed‑данные при первом запуске.

Приложение Г — Исходный код канбан‑сервиса (`kanban_service.py`, `kanban_models.py`). Содержит: создание, чтение, обновление, удаление задач, перемещение между колонками, фильтрацию по ролям (`get_tasks_for_user`), отслеживание просрочки, статистику, уведомления, экспорт CSV, синхронизацию с 1С.

Приложение Д — Скриншоты интерфейса расширений в 1С на ПК (таблица чата с условным оформлением, канбан‑доска в отдельном окне, быстрые кнопки) и примеры диалогов.

Приложение Е — Руководство пользователя

Е.1 Установка и запуск middleware

1) Клонировать репозиторий проекта или скопировать каталог Middleware на рабочую станцию.
2) Установить Python 3.12+ (https://python.org).
3) В каталоге Middleware выполнить: `python -m venv .venv` — создание виртуального окружения.
4) Активировать окружение: `.venv\Scripts\activate` (Windows) или `source .venv/bin/activate` (Linux).
5) Установить зависимости: `pip install -r requirements.txt`.
6) Создать файл `.env` в каталоге Middleware со строкой: `FIREWORKS_API_KEY=your_key_here`.
7) Запустить сервер: `python -m uvicorn main:app --host 127.0.0.1 --port 8000`.
8) Проверить работоспособность: открыть в браузере `http://127.0.0.1:8000/health` — должен вернуться JSON `{"status": "ok"}`.

Е.2 Загрузка расширений в 1С:УНФ

1) Открыть конфигуратор 1С:УНФ.
2) Перейти в меню «Конфигурация» → «Расширения конфигурации».
3) Создать новое расширение: имя «Ассистент», префикс «АС_».
4) В модуле формы обработки создать форму «Форма» с реквизитами: АдресMiddleware (Строка, 100, по умолчанию «127.0.0.1»), ЛогинMiddleware (Строка, 100, по умолчанию «1c_service»), ПарольMiddleware (Строка, 100, по умолчанию «1c_service_2024»), ИспользоватьHTTPS (Булево, по умолчанию Ложь), MiddlewareТокен (Строка, 500), ЧатHTML (Строка, неограниченная), ТекстСообщения (Строка, 500), ФлагЗагрузки (Булево), табличный реквизит ЧатИстория (Автор, Текст, Время, ТипСообщения).
5) Скопировать код из файла `1C_Full_Code_v6.bsl` в модуль формы.
6) Аналогично создать расширение «КанбанДоска» (префикс «КД_») с формой «Форма» (реквизиты: АдресMiddleware, ЛогинMiddleware, ПарольMiddleware, ИспользоватьHTTPS, MiddlewareТокен, КанбанHTML, ПолеФильтр, ФильтрПоДепартаменту) и формой «МобильнаяФорма» (реквизит ЗадачиТаблица с колонками ИдЗадачи, Заголовок, Статус, Приоритет, Исполнитель, Дедлайн, Просрочено).
7) Скопировать код из файла `1C_Kanban_Extension.bsl` в модуль формы канбан‑доски.
8) Снять флаг «Безопасный режим» для обоих расширений (требуется для выполнения HTTP‑запросов).
9) Сохранить и обновить конфигурацию базы данных.

Е.3 Первый запрос

1) Открыть 1С:УНФ в режиме предприятия.
2) В панели навигации найти обработку «Чат с ассистентом» (или открыть через меню «Операции» → «Обработки»).
3) В поле ввода набрать «Привет» или «Открой список заказов» и нажать кнопку отправки.
4) При первом запросе расширение автоматически авторизуется на middleware и получит JWT‑токен.
5) Ответ ассистента отобразится в чате в виде «бабла» на тёмно‑синем фоне с аватаром «AI».

Е.4 Быстрые кнопки

- «📋 Заказы» — открывает список заказов покупателей.
- «📌 Канбан» — открывает канбан‑доску в отдельном окне.
- «📊 Сегодня» — запрашивает продажи за текущий день.
- «💰 Должники» — формирует список дебиторов.
- «👤 Клиенты» — открывает справочник контрагентов.

Е.5 Канбан‑доска

1) Нажать кнопку «📌 Канбан» в форме чата — откроется отдельное окно канбан‑доски.
2) Доска отображает четыре колонки: «К выполнению», «В работе», «На проверке», «Готово».
3) Для создания задачи нажать кнопку «Создать задачу», ввести заголовок и нажать Enter.
4) Для перемещения задачи — кликнуть по карточке, выбрать действие из контекстного меню.
5) Для фильтрации — ввести текст в поле «Фильтр по задаче».
6) Также задачу можно создать через чат: «Добавь задачу: подготовить отчёт для Иванова до пятницы».

Е.6 Обновление метаданных

1) В форме чата нажать кнопку «Обновить метаданные» (если добавлена) или ввести команду «Обнови метаданные» в чате.
2) Расширение выполнит обход объектов конфигурации (справочники, документы, отчёты) и отправит пакет в middleware.
3) Middleware обновит JSON‑индекс и предвычисленные эмбеддинги.
4) После обновления ассистент будет учитывать новые объекты конфигурации при поиске.

Е.7 Мобильный клиент

1) Опубликовать базу 1С на веб‑сервере (Apache/IIS) через утилиту `webinst`.
2) Установить мобильное приложение «1С:Предприятие» на Android‑устройство.
3) Подключиться к опубликованной базе по URL (http://server/base_name).
4) Расширения чата и канбан‑доски автоматически загрузятся; интерфейс адаптирован под мобильный клиент.

Е.8 Устранение типичных проблем

- «Не удалось загрузить канбан‑доску» / «Ошибка связи с ассистентом» — убедитесь, что middleware запущен (`http://127.0.0.1:8000/health` возвращает OK).
- Ошибка 401 при каждом запросе — проверьте учётные данные в реквизитах ЛогинMiddleware и ПарольMiddleware формы; убедитесь, что пользователь 1c_service существует в базе middleware (создаётся автоматически при первом запуске).
- Пустой ответ ассистента — проверьте, что FIREWORKS_API_KEY указан в файле .env и ключ действителен.
- Канбан‑доска открывается, но пуста — убедитесь, что в middleware созданы задачи (GET /kanban/tasks возвращает данные).
