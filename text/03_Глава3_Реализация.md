3 РЕАЛИЗАЦИЯ РАСШИРЕНИЯ И СЕРВЕРНОЙ ЧАСТИ

3.1 Реализация логики диалога и протокола команд

Диалог с интеллектуальным ассистентом основан на обмене структурированными JSON-пакетами. На каждый запрос пользователя middleware обращается к LLM (модели GLM-5.1 через Fireworks API), передавая историю диалога и RAG-контекст. Ответ модели приводится к фиксированному формату:
```json
{
  "text": "Текст ответа для пользователя",
  "action": "имя_действия_из_whitelist",
  "data": { "параметр1": "значение1" }
}
```
Для повышения надежности в middleware реализован многоуровневый извлекатель JSON (поиск блоков ` ```json ` и балансировка фигурных скобок), дополненный эвристической постобработкой намерений (если модель возвращает текстовый ответ, но в запросе явно упоминаются команды канбан-доски).

3.2 Реализация интерфейса и клиентской логики в 1С

Чат-интерфейс в 1С реализован как обработка в расширении конфигурации. Используется гибридная схема:
- HTML-поле формы `ЧатHTML` выводит диалог в виде бабблов (оранжевый цвет для пользователя, темно-синий для ассистента, анимированный индикатор печати на время ожидания HTTP-ответа).
- Табличная часть управляемой формы `ЧатИстория` выступает в роли хранилища истории диалога для отправки в middleware.

Диспетчер команд `ВыполнитьКомандуАссистента()` анализирует поле `action` и вызывает соответствующие прикладные методы. При действии `show_kanban` ассистент автоматически определяет тип клиента (мобильный/десктоп) и открывает соответствующую форму.

Создание документов в 1С (действие `create_object`) реализовано по шаблону Chain of Responsibility. Цепочка поочередно проверяет существование контрагента и номенклатуры в базе. При отсутствии элемента пользователю предлагается модальный вопрос о его создании с предзаполненными реквизитами, и только после этого открывается форма нового документа.

3.2.1 Ключевые фрагменты кода клиентской части

Ниже приведены основные процедуры и функции на встроенном языке 1С, обеспечивающие работу чата, отправку запросов по HTTP и выполнение цепочки создания объектов.

Фрагмент: отправка сообщения с историей диалога — процедура `ОтправитьСообщение`
```bsl
&НаКлиенте
Процедура ОтправитьСообщение(Команда)
    Если ПустаяСтрока(ТекстСообщения) Тогда Возврат; КонецЕсли;
    ЗапросТекст = ТекстСообщения; ТекстСообщения = "";
    ДобавитьСообщениеВЧатНаСервере(ЗапросТекст, "user");
    ФлагЗагрузки = Истина; ЧатHTML = СгенерироватьЧатHTMLЗагрузка();
    
    // Формирование JSON-истории (до 20 сообщений)
    МассивСообщений = Новый Массив;
    КоличествоСтрок = ЧатИстория.Количество();
    НачИндекс = ?(КоличествоСтрок > 20, КоличествоСтрок - 20, 0);
    Для Инд = НачИндекс По КоличествоСтрок - 1 Цикл
        СтрЧата = ЧатИстория[Инд];
        РольСообщения = ?(СтрЧата.ТипСообщения = "user", "user", "assistant");
        МассивСообщений.Добавить(Новый Структура("role, text", РольСообщения, СтрЧата.Текст));
    КонецЦикла;
    МассивСообщений.Добавить(Новый Структура("role, text", "user", ЗапросТекст));
    
    СтруктураЗапроса = Новый Структура("messages, context", МассивСообщений, "Форма: " + ЭтаФорма.ИмяФормы);
    СтрокаJSON = ПолучитьСтрокуJSON(СтруктураЗапроса); // Вспомогательный метод сериализации
    
    Попытка
        HTTPОтвет = ВыполнитьHTTPЗапросНаСервере("/chat", СтрокаJSON);
        ФлагЗагрузки = Ложь;
        Если HTTPОтвет = Неопределено Тогда
            ДобавитьСообщениеВЧатНаСервере("Сервер недоступен. Запустите: python main.py", "bot"); Возврат;
        КонецЕсли;
        
        Если HTTPОтвет.КодСостояния = 200 Тогда
            СтруктураОтвета = ПрочитатьJSONСтроку(HTTPОтвет.ПолучитьТелоКакСтроку());
            ДобавитьСообщениеВЧатНаСервере(СтруктураОтвета.text, "bot");
            
            Действие = "";
            Если СтруктураОтвета.Свойство("action", Действие) И ЗначениеЗаполнено(Действие) Тогда
                Данные = Неопределено; СтруктураОтвета.Свойство("data", Данные);
                ВыполнитьКомандуАссистента(Действие, Данные);
            КонецЕсли;
        Иначе
            ДобавитьСообщениеВЧатНаСервере("Ошибка сервера: " + HTTPОтвет.КодСостояния, "bot");
        КонецЕсли;
    Исключение
        ФлагЗагрузки = Ложь; ДобавитьСообщениеВЧатНаСервере("Ошибка: " + ОписаниеОшибки(), "bot");
    КонецПопытки;
КонецПроцедуры
```

Фрагмент: диспетчер команд — процедура `ВыполнитьКомандуАссистента`
```bsl
&НаКлиенте
Процедура ВыполнитьКомандуАссистента(Действие, Данные)
    Если Данные = Неопределено ИЛИ ТипЗнч(Данные) <> Тип("Структура") Тогда Возврат; КонецЕсли;
    ТипОбъекта = ""; Данные.Свойство("type", ТипОбъекта);
    
    Если Действие = "create_kanban_task" Тогда
        ВыполнитьСозданиеКанбанЗадачи(Данные);
    ИначеЕсли Действие = "show_kanban" Тогда
        Попытка
            Если ЭтоМобильныйКлиент() Тогда
                ОткрытьФорму("Обработка.КанбанДоска.МобильнаяФорма");
            Иначе
                ОткрытьФорму("Обработка.КанбанДоска.Форма");
            КонецЕсли;
        Исключение
            ДобавитьСообщениеВЧатНаСервере("Расширение КанбанДоска не установлено.", "bot");
        КонецПопытки;
    ИначеЕсли Действие = "open_list" Тогда
        БезопасноОткрытьФорму(ТипОбъекта + ".ФормаСписка", "Список " + ТипОбъекта);
    ИначеЕсли Действие = "open_form" Тогда
        БезопасноОткрытьФорму(ТипОбъекта + ".ФормаОбъекта", ТипОбъекта);
    ИначеЕсли Действие = "create_object" Тогда
        Поля = Неопределено;
        Если Данные.Свойство("fields", Поля) И ТипЗнч(Поля) = Тип("Структура") Тогда
            РезультатПодготовки = ПодготовитьДанныеЗаполненияНаСервере(ТипОбъекта, Поля);
            КонтекстСоздания = Новый Структура("ТипОбъекта, РезультатПодготовки", ТипОбъекта, РезультатПодготовки);
            Шаг1_ПроверкаКлиента(КонтекстСоздания);
        КонецЕсли;
    КонецЕсли;
КонецПроцедуры
```

Фрагмент: цепочка создания объектов — процедуры `Шаг1_ПроверкаКлиента` и `Шаг3_Финал`
```bsl
&НаКлиенте
Процедура Шаг1_ПроверкаКлиента(Контекст)
    Данные = Контекст.РезультатПодготовки;
    Если Данные <> Неопределено И Данные.Свойство("НеНайденКлиент") Тогда
        ИмяКлиента = Данные.НеНайденКлиент.Наименование;
        Оповещение = Новый ОписаниеОповещения("ПослеОтветаОКлиенте", ЭтотОбъект, Контекст);
        ПоказатьВопрос(Оповещение, "Клиент '" + ИмяКлиента + "' не найден. Создать?", РежимДиалогаВопрос.ДаНет);
    Иначе
        Шаг2_ПроверкаТовара(Контекст);
    КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура Шаг3_Финал(Контекст)
    ПараметрыЗаполнения = Новый Структура;
    Если Контекст.РезультатПодготовки <> Неопределено И Контекст.РезультатПодготовки.Свойство("ЗначенияЗаполнения") Тогда
        ПараметрыЗаполнения = Контекст.РезультатПодготовки.ЗначенияЗаполнения;
    КонецЕсли;
    ПараметрыФормы = Новый Структура("ЗначенияЗаполнения", ПараметрыЗаполнения);
    ОткрытьФорму(Контекст.ТипОбъекта + ".ФормаОбъекта", ПараметрыФормы);
КонецПроцедуры
```

Фрагмент: сбор метаданных — функция `СобратьМетаданныеНаСервере`
```bsl
&НаСервере
Функция СобратьМетаданныеНаСервере()
    Список = Новый Массив; Коллекции = Новый Массив;
    Коллекции.Добавить("Справочники"); Коллекции.Добавить("Документы"); Коллекции.Добавить("Отчеты");
    Для Каждого ИмяКоллекции Из Коллекции Цикл
        Для Каждого МетаОбъект Из Метаданные[ИмяКоллекции] Цикл
            ИмяТипа = ?(ИмяКоллекции="Справочники", "Справочник", ?(ИмяКоллекции="Документы", "Документ", "Отчет"));
            МассивПолей = Новый Массив;
            Для Каждого Рек Из МетаОбъект.Реквизиты Цикл
                МассивПолей.Добавить(Новый Структура("name, synonym, type", Рек.Имя, Рек.Синоним, Строка(Рек.Тип)));
            КонецЦикла;
            Список.Добавить(Новый Структура("name, synonym, fields", ИмяТипа + "." + МетаОбъект.Имя,
                ?(ПустаяСтрока(МетаОбъект.Синоним), МетаОбъект.Имя, МетаОбъект.Синоним), МассивПолей));
        КонецЦикла;
    КонецЦикла;
    ЗаписьJSON = Новый ЗаписьJSON; ЗаписьJSON.УстановитьСтроку();
    ЗаписатьJSON(ЗаписьJSON, Новый Структура("items", Список));
    Возврат ПоместитьВоВременноеХранилище(ЗаписьJSON.Закрыть(), УникальныйИдентификатор);
КонецФункции
```

3.3 Реализация middleware

Серверная часть разработана на Python с использованием фреймворка FastAPI. Хранение канбан-задач, пользователей и департаментов реализовано в реляционной базе данных SQLite, доступ к которой осуществляется через SQLAlchemy ORM.

3.3.1 Реализация авторизации

Авторизация построена на базе JWT-токенов (подпись алгоритмом HS256, секретный ключ хранится в переменной окружения `JWT_SECRET`). При каждом запросе токен извлекается из заголовка `X-Auth-Token` и валидируется. На основе JWT-декодирования извлекается роль пользователя (`admin`, `manager`, `employee`) и его департамент, что используется в ролевой фильтрации данных.

3.3.2 Ключевые фрагменты серверной части

Ниже приведены ключевые участки серверного кода, реализующие RAG-интеграцию, санитизацию JSON и ролевую фильтрацию задач.

Фрагмент: генерация ответа с RAG-поиском — `llm_service.py`
```python
async def generate_response(self, messages: list, context: str = None) -> dict:
    user_message_text = messages[-1]["text"]
    # Вызов семантического поиска
    candidates = metadata_service.find_top_matches(user_message_text, top_k=7)
    
    candidates_str = "ДОСТУПНЫЕ ОБЪЕКТЫ 1С:\n" if candidates else "Список объектов пуст."
    for c in (candidates or []):
        field_parts = [f"{f['name']}[{f.get('type','')}]" for f in c.get("fields", [])[:8]]
        fields_str = f" (Поля: {', '.join(field_parts)})" if field_parts else ""
        candidates_str += f"- {c['name']} (Синоним: {c['synonym']}){fields_str}\n"

    system_prompt = self.base_prompt + "\n" + candidates_str
    if context:
        system_prompt += f"\nТЕКУЩИЙ КОНТЕКСТ 1С: {context}"
    
    # Обращение к Fireworks API
    response = await self.client.chat.completions.create(
        model=self.model_name,
        messages=[{"role": "system", "content": system_prompt}] + messages
    )
    generated_text = response.choices[0].message.content
    return self.parse_and_postprocess(generated_text, user_message_text)
```

Фрагмент: извлечение и санитизация JSON — `llm_service.py`
```python
def _sanitize_json(self, text: str) -> str:
    text = re.sub(r'//.*?$', '', text, flags=re.MULTILINE) # Удаление комментов //
    text = re.sub(r'/\*.*?\*/', '', text, flags=re.DOTALL)    # Удаление комментов /* */
    text = re.sub(r",\s*([}\]])", r"\1", text)               # Лишние запятые
    text = re.sub(r"'", '"', text)                           # Одинарные кавычки
    return re.sub(r'(\w+)\s*:', r'"\1":', text)              # Ключи без кавычек

def _extract_json(self, text: str) -> dict | None:
    # 1. Попытка парсинга блока ```json
    match = re.search(r'```json\s*(.*?)\s*```', text, re.DOTALL)
    if match:
        for attempt in (match.group(1), self._sanitize_json(match.group(1))):
            try: return json.loads(attempt)
            except json.JSONDecodeError: pass

    # 2. Поиск сбалансированных фигурных скобок
    start = text.find('{')
    if start == -1: return None
    depth, candidates = 0, []
    for i, ch in enumerate(text[start:], start):
        if ch == '{': depth += 1
        elif ch == '}': depth -= 1
        if depth == 0:
            candidates.append(text[start:i+1])
            start = text.find('{', i + 1)
            if start == -1: break
            depth = 0

    for candidate in candidates:
        for attempt in (candidate, self._sanitize_json(candidate)):
            try: return json.loads(attempt)
            except json.JSONDecodeError: pass
    return None
```

Фрагмент: фильтрация канбан-задач по ролям — `kanban_service.py`
```python
def get_tasks_for_user(self, user_id: int, role: str, department_id: int = None,
                       username: str = None, filter_dept: int = None) -> List[dict]:
    session = self._get_session()
    try:
        q = session.query(KanbanTaskDB)
        # Ролевое разграничение
        if role == "employee" and username:
            q = q.filter(or_(KanbanTaskDB.assignee == username, KanbanTaskDB.created_by == username))
        elif role == "manager" and department_id:
            q = q.filter(or_(KanbanTaskDB.department_id == department_id,
                             KanbanTaskDB.assignee == username,
                             KanbanTaskDB.created_by == username))
        
        if filter_dept is not None:
            q = q.filter(KanbanTaskDB.department_id == filter_dept)
            
        tasks = q.order_by(KanbanTaskDB.column, KanbanTaskDB.position).all()
        return self._enrich_tasks(tasks, session)
    finally:
        session.close()
```

3.3.3 Реализация канбан-сервиса

Канбан-сервис отвечает за управление задачами в БД. Задачи содержат поля `title`, `description`, `column`, `priority` (low/medium/high/urgent), `assignee` и `department_id`.
При запросе списка задач автоматически рассчитывается флаг `is_overdue` (если срок просрочен и задача не находится в колонке «done»). Эндпоинт `/kanban/stats` возвращает агрегированную информацию по колонкам, приоритетам и департаментам, а `/kanban/notifications` формирует сообщения для пользователей о новых или просроченных задачах.

3.3.4 Реализация механизма семантического поиска

RAG-поиск по метаданным 1С реализован на основе сравнения косинусного сходства векторов. База векторов (384 измерения, MiniLM) вычисляется при обновлении метаданных и сохраняется в файле `metadata_embeddings.npy`. Для систем без sentence-transformers реализован fallback-поиск по пересечению слов (Word Overlap).

3.4 Система защиты и контроль рисков

Для безопасного функционирования системы применены следующие меры:
- Whitelist-модель команд: LLM может инициировать только команды из белого списка.
- Аутентификация: все запросы к API middleware защищены JWT-токеном с проверкой ролей на уровне SQL-запросов.
- Локальный контур: 1С взаимодействует с middleware по адресу `127.0.0.1`, исключая внешние сетевые атаки.
- Контроль секретов: API-ключи LLM хранятся в `.env` и не попадают в код.

3.5 Тестирование

Тестирование разработанного решения выполнено с использованием автоматизированных тестов (pytest) и ручных сценариев.

3.5.1 Методика и результаты тестирования

Было разработано и запущено 45 автоматизированных тестов, охватывающих авторизацию, чат, канбан-сервис и интеграцию с 1С. Все тесты пройдены успешно (таблица 3.1).

Таблица 3.1 — Сводка результатов автоматизированного тестирования (pytest)

| Группа тестов | Кол-во | Описание | Результат |
|---|---|---|---|
| AUTH (Авторизация) | 14 | Проверка регистрации, логина, создания JWT, верификации прав admin/manager/employee. | Пройдено |
| CHAT (Диалог и RAG) | 5 | Проверка отправки сообщений, семантического RAG-подбора и извлечения JSON. | Пройдено |
| KAN (Канбан-сервис) | 10 | CRUD задач, расчет статистики, генерация нотификаций и экспорт в CSV. | Пройдено |
| INT (Интеграция 1С) | 16 | Имитация ответов 1С, цепочка проверок при создании объектов, авторетрай при 401 ошибке. | Пройдено |
| **Итого** | **45** | | **Пройдено 100%** |

Дополнительно проведено ручное тестирование на развернутой системе по 10 ключевым сценариям (таблица 3.2).

Таблица 3.2 — Результаты выполнения ручных сценариев тестирования

| Код | Сценарий | Шаги тестирования | Результат |
|---|---|---|---|
| РС-01 | Открытие списка | Ввод запроса «Открой заказы» | Открывается список документов в 1С. |
| РС-02 | Проверка статуса | Запрос «Проведен ли заказ 32?» | В чат выводится текущий статус документа. |
| РС-03 | Остатки товаров | Запрос «Сколько цемента на складе?» | Выводится точное количество остатков. |
| РС-04 | Список должников | Запрос «Покажи должников» | Отображается таблица взаиморасчетов. |
| РС-05 | Создание заказа | Запрос «Создай заказ для ООО Ромашка на 10 цемента» | Вызывается цепочка проверок, открывается форма нового предзаполненного заказа. |
| РС-06 | Добавление задачи | Запрос «Добавь задачу подготовить отчет» | Задача создается на доске канбан, карточка выводится в чат. |
| РС-07 | Открытие доски | Нажатие кнопки «📌 Канбан» | Загружается интерактивная доска задач. |
| РС-08 | Мобильный клиент | Выполнение запросов на Android-устройстве | Интерфейс адаптируется, команды отрабатывают. |
| РС-09 | Контроль просрочки | Создание просроченной задачи | На доске появляется красная метка «Просрочено!». |
| РС-10 | Rate Limiting | Имитация DoS-атаки (>30 запросов в минуту) | Блокировка запросов с кодом 429. |

Оценка покрытия функциональных требований (FR-01 – FR-10) тестами AUTH-01…INT-16 и ручными сценариями РС-01…РС-10 показала 100%-е покрытие всех заявленных требований. Среднее время генерации ответа ассистента на типовые запросы составило 4–6 секунд, при этом время обработки навигационных команд на стороне 1С не превышает 1 секунды.
