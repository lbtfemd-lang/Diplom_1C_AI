fig_2_3 — Структура элемента метаданных + RAG

Холст: 2000×900

Заголовок:
Рисунок 2.3 — Структура элемента метаданных и место в процессе RAG

Левый блок (x=80, y=130, w=480, h=520): «ЭЛЕМЕНТ МЕТАДАННЫХ (из 1С)», оранжевая обводка
Содержимое (моноширинный шрифт, выровнено влево с отступом):
  name:          Документ.ЗаказПокупателя
  synonym:       Заказ покупателя

  fields[0]:
    name:        Контрагент
    synonym:     Контрагент
    type:        СправочникСсылка.Контрагенты   ← фиолетовый

  fields[1]:
    name:        СуммаДокумента
    synonym:     Сумма документа
    type:        Число(15, 2)                    ← фиолетовый

Горизонтальная цепочка (y центра = 350):
5 блоков в ряд, соединённых стрелками:

1. Энкодер (фиолетовый): paraphrase-multilingual / MiniLM-L12-v2 (384d) / SentenceTransformers
2. → стрелка с подписью «384d вектор» над ней
3. Вектор (синий): 384 измерения / Cosine similarity / 1423 объекта
4. → стрелка
5. Top-7 (фиолетовый): кандидатов / name + synonym / + fields[type]

Стрелка от левого блока к Энкодеру с подписью «Векторизация» сверху.

Нижний ряд (ниже цепочки):
Стрелка вниз от Top-7 к блоку LLM (GLM-5.1) (зелёный): System prompt + RAG + few-shot

Стрелка от LLM влево к блоку «Разбор JSON» (синий): + whitelist, + post-processing
Подпись к стрелке LLM→Разбор: JSON {text, action, data}
